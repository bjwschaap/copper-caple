package caple

// Version of the copper-caple service
const Version = "0.1.0"

const apiVersion = "v1"
const contextPrefix = "/caple"
const contextPath = contextPrefix + "/" + apiVersion
const loggerPrefix = "[copper-caple] "
